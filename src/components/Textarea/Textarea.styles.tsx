import styled from 'styled-components';

import { TextField, Root as TextfieldRoot } from '../Textfield/Textfield.styles';

export const Root = styled(TextfieldRoot)``;

export const Textarea = styled(TextField)`
  height: 6rem;
`;
